#Questao1
Correto
Nota: 1,00

#Questao2
Correto
Nota: 1,00

#Questao3
Parcialmente correto
Nota: 0,50

#Questao4
Correto
Nota: 1,00

#Questao5
Correto
Nota: 1,00

#Questao6
Correto
Nota: 1,00

#Questao7
Correto
Nota: 1,00

#Questao8
Incorreto
Nota: 0,00

#Questao9
Correto
Nota: 1,00

#Questao10
Correto
Nota: 1,00

Subtotal: 8,50

Penalizações:
- Nome do repositório remoto incorreto para a turma (`checkpoint-1-webdev-1ESS` em vez de `checkpoint-1-webdev-1ESPZ` — reprodução do exemplo do material em vez de substituir pelo identificador da turma): −0,50
- Colaborador: não verificável.
- Commits: um integrante com commits na `main` — atende ao mínimo por integrante.
- `participantes.md`: ok.

Total final: 8,00

🧾 EXPLICAÇÃO DA CORREÇÃO

*Quando a entrega cumpre o objetivo pedido na questão, foi considerada **correta**, mesmo sem repetir o enunciado ao pé da letra (exceto quando o requisito objetivamente não foi atendido).*

**Questão 1 — Ambiente:** Comentários com `node`, `npm` e `git --version`.

**Questão 2 — HTML + JS inicial:** `<h1>` Checkpoint #1, `console.log("Mensagem inicial")`, `alert` de boas-vindas e `<script src="script.js">` antes de `</body>`.

**Questão 3 — Repositório e commit:** Há histórico coerente (`Initial commit` e commit da Q3), mas o comentário da Q3 limita-se a afirmar que o repo foi criado e houve commit inicial, **sem descrever** o repositório (nome final, propósito ou local no Git) como o enunciado pede — parcial.

**Questão 4 — Variáveis:** Duas `var`, duas `let`, duas `const` e valores iniciais visíveis no console.

**Questão 5 — Aritmética:** Duas variáveis numéricas e as quatro operações no console.

**Questão 6 — Comparação:** `==`, `===`, `!=`, `!==`, `>` e `<` com logs dos booleanos.

**Questão 7 — Lógicos:** `&&`, `||` e `!` no console.

**Questão 8 — Branch e merge:** Comentário “Exemplo de branch e merge” presente, porém em `git branch -a` só existe `main` / `origin/main` — não há branch de trabalho nem merge evidenciados — incorreto.

**Questão 9 — compararNumeros:** `if`/`else if` cobrindo maior, menor e igual; chamadas com (5,10), (10,5) e (7,7).

**Questão 10 — Operadores no navegador:** Soma com rótulo “Soma:” e expressões `&&` / `||` com booleanos exibidas no console.

*Penalizações aplicadas:* desconto de 0,50 pelo nome do repositório no GitHub não seguir `checkpoint-1-webdev-{turma}` (`1ESS` copiado do exemplo em vez de `1ESPZ`).

---

«CONTEXTO GIT»: `origin` → `checkpoint-1-webdev-1ESS.git` (esperado para esta turma: `checkpoint-1-webdev-1ESPZ`); apenas `main`; dois commits por `Guilherme-Padovani`.
